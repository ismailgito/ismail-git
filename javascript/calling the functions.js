function calculateBottles(startingMoney,costPerBottles) {
    var numberOfBottles=Math.floor(startingMoney.costPerBottles);
    return numberOfBottles;
}
function calChange(staringAmount,costPerPrice) {
    var change=startingPpoint%costPerBottles;
    return change;
}
console.log("hello master,here is your"+getMilk(5)+"change");